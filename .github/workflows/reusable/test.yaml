name: Shared Tests

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string

      working_directory:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "App Name: ${{ inputs.app_name }}"
          echo "Working Directory: ${{ inputs.working_directory }}"
